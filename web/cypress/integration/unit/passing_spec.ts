/**
 * Will always return true. We run this to test unit test integration
 *
 * * Feel free to delete this test
 */
describe('Initial Test', () => {
  it('Returns true', () => {
    expect(true).to.equal(true)
  })
})
